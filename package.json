{
  "name": "ewg-booking-agent",
  "version": "1.0.0",
  "description": "Elite World Group — AI Booking Agent Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "scrapingbee": "^1.5.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
